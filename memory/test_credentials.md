# Test Credentials — Sijill Project

## Admin / Demo (utilisateur principal)
- Email: `loubna.serrar@gmail.com`
- Password: `Admin123!`
- Role: `admin`
- Used for: Admin panel access (`/api/admin-panel/`), demo account testing, all backend admin endpoints (`/api/admin/*`).

## Notes
- Created by seed routine in `backend/server.py`. Idempotent across restarts.
- Update this file every time auth credentials are added or modified.
